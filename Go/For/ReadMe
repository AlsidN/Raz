              Одна из частей собственной реализации языка Forth    (в процессе написания).
                   
 - позволяет выполнять простые математические операции с числами ('+','-','*','/')
    '+' - берет числа из стека и складывает их.Результат помещает в стек
    '-' - берет числа из стека и отнимает из первого второе их.Результат помещает в стек
    '*' - берет числа из стека и умножает их.Результат помещает в стек
    '/' - берет числа из стека и делит их.Результат помещает в стек
   
   Пример:
    1. Вводите число 
    2. Вводите второе число
    3. Вводите мат.операцию
    4. Результат выводится командой .s
      Можно ввести строкой: 1 2 + .s
      
 -позволяет общаться с помощью слов.Ввести :
   1.  : (для определения) с пробелом в конце
   2.  основное название с пробелом в конце
   3.  .' (для начало сообщения с пробелом в конце)
   4.  сообщение с пробелом в конце
   5.  ; (для завершения ввода переменной)
     Пример:
   >  : a .' Здрасть ;
   >  a 
   >  Здрасть (покажется доп.информация)
    
   Команды :
   1. +
   2. -
   3. *
   4. /
   5. .s - выводи содержимое стека
   6.  . - удаляет из стека число и выводит на экран
   7. cls -очищает полностью стек
   8. show -показывает список слов
   9  dup  - делает копию числа в стеке
   10. exit -выход из программы
   11. emit - выводит символ ASCII по числу в формате dec
      Пример:
        > : star 42 emit ;
        > star
        > *
   12. do и loop задают цикл (выведет что-либо нужное количество раз)
       Первое число показывает верхний предел инкремента.
       Второе число показывает нижний предел инкремента.
       
       Инкремент идет от большего числа к меньшему.
       Если числа поменять местами, то покажет один символ      
      Пример:
        > : qwe 3 0 do star loop ;
        > qwe
        > *
        > *
        > *
  
    
     
     
   
 
